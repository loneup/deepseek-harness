/** Node face for the browser-only Nexus settings package. */
export const name = 'ui-settings-nexus'
export const inject: readonly string[] = []
export function apply(): void {}
/** Cordis 插件默认导出：浏览器-only 包的空节点桩。 */
export default { name, inject, apply }
